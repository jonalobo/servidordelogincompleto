/* const { Router } = require('express')

const rutaIngresar = Router()

rutaIngresar.get('/')
rutaIngresar.post('/')
rutaIngresar.put('/')
rutaIngresar.patch('/')
rutaIngresar.delete('/')

module.exports = rutaIngresar */